# This monorepo uses TWO env files, one per app:
#   apps/api/.env   ->  copy from apps/api/.env.example   (database, auth, email, storage)
#   apps/web/.env   ->  copy from apps/web/.env.example   (API URL / dev proxy)
#
# There is no root .env. See README.md → "Configuration" for the full list.
